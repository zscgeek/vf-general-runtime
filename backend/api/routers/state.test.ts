import {describe, test} from '@jest/globals';

describe('Dialog API - Stateful', () => {
  describe('POST /state/user/:userID/interact', () => {
    test.todo('');
  });

  describe('/state/:versionID/user/:userID/interact', () => {
    test.todo('');
  });
});
